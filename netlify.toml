[build]
  command = "pnpm install && pnpm --filter @workspace/initia-vienne run build"
  publish = "artifacts/initia-vienne/dist/public"

[build.environment]
  NODE_VERSION = "24"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
